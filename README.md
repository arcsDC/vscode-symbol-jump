# vscode-symbol-jump

A VS Code extension that jumps to the next or previous symbol in the active file using keyboard shortcuts.

## Stack

Primary language: **TypeScript**

## Project structure

- `src/extension.ts` — Navigate between symbols in the current document.
- `package.json` — extension manifest

## Usage

Review the source files and install any dependencies referenced by the project before running it.
